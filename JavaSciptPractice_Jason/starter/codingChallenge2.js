if (isMarkHigherBMI)
  console.log(
    // "Mark's BMI (" + BMIMark + ") is higher than John's (" + BMIJohn + ")!"
    `Mark's BMI (${BMIMark}) is higher than John's ( ${BMIJohn} )❤️!`
  );
else
  console.log(`Mark's BMI (${BMIMark}) is lesser than John's ( ${BMIJohn} )!`);
